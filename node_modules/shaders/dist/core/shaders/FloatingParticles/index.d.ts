import { GpuShaderDefinition } from '../../gpu/porters';
import { d } from '../../gpu/kit';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Randomness of particle animation
     *
     * @default 0.25
     */
    randomness: number;
    /**
     * Speed of particle movement
     *
     * Accepts a number between 0 and 1.
     * @default 0.25
     */
    speed: number;
    /**
     * Movement angle in degrees (0=right, 90=down, 180=left, 270=up)
     *
     * Accepts a number between 0 and 360.
     * @default 90
     */
    angle: number;
    /**
     * Size of particles
     *
     * @default 2
     */
    particleSize: number;
    /**
     * Edge softness of particles (0 = sharp, 1 = very soft)
     *
     * @default 0
     */
    particleSoftness: number;
    /**
     * Intensity of the twinkle effect (0 = off, 1 = full twinkle)
     *
     * @default 0.5
     */
    twinkle: number;
    /**
     * Number of particle layers
     *
     * Accepts a number between 1 and 5.
     * @default 5
     */
    count: number;
    /**
     * Color of the particles
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    particleColor: Parameters<typeof transformColor>[0];
    /**
     * Per-layer speed variance (0 = all layers same speed, 1 = high variance)
     *
     * Accepts a number between 0 and 1.
     * @default 0.3
     */
    speedVariance: number;
    /**
     * Per-layer angle variance in degrees (0 = all layers same angle, 180 = full variance)
     *
     * Accepts a number between 0 and 180.
     * @default 30
     */
    angleVariance: number;
    /**
     * Particle density (lower = more spread out, higher = more dense)
     *
     * Accepts a number between 0.5 and 3.
     * @default 3
     */
    particleDensity: number;
}
/** Compute kernel: render the layered particle field for one texel of the fixed-res texture. */
export declare const particleKernel: import('typegpu').TgpuFn<(cx: d.U32, cy: d.U32) => d.Void>;
export declare const particleTapUV: import('typegpu').TgpuFn<(uv: d.Vec2f, ox: d.F32, oy: d.F32) => d.Vec2f>;
/** Weighted (centre 0.5 + 4 neighbours 0.125) upsample of the point-stored field. */
export declare const particleUpsample: import('typegpu').TgpuFn<(s0: d.Vec4f, s1: d.Vec4f, s2: d.Vec4f, s3: d.Vec4f, s4: d.Vec4f) => d.Vec4f>;
/** Composite the particles OVER a child colour (mix + straight-alpha over). */
export declare const particleOverChild: import('typegpu').TgpuFn<(p: d.Vec4f, c: d.Vec4f) => d.Vec4f>;
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map